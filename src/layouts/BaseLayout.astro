---
interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'LanDeng — Chinese Botanical Incense',
  description = 'A quiet, research-led knowledge platform for Chinese botanical incense.',
} = Astro.props;

const SITE = Astro.site?.toString() ?? 'https://example.com';
const canonical = Astro.site
  ? new URL(Astro.url.pathname, Astro.site).toString()
  : `${SITE}${Astro.url.pathname}`;
const ogImage = new URL('images/og-image-incense-still-life-1200x630.jpg', `${SITE}/`).toString();

const navItems = [
  { href: '/', label: 'Home', exact: true },
  { href: '/ingredients/', label: 'Ingredients' },
  { href: '/wholesale/', label: 'Wholesale' },
  { href: '/oem/', label: 'OEM' },
  { href: '/contact/', label: 'Contact' },
];
const currentPath = Astro.url.pathname;

const orgJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  '@id': `${SITE}#organization`,
  name: 'LanDeng',
  alternateName: '澜灯',
  url: SITE,
  description: 'Chinese botanical incense knowledge platform.',
};

const webSiteJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  '@id': `${SITE}#website`,
  url: SITE,
  name: 'LanDeng',
  publisher: { '@id': `${SITE}#organization` },
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <meta name="generator" content={Astro.generator} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Source+Serif+4:opsz,wght@8..60,400;8..60,600;8..60,700&family=Noto+Serif+SC:wght@400;600&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
    <meta property="og:site_name" content="LanDeng" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImage} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <script type="application/ld+json" set:html={JSON.stringify(orgJsonLd)} />
    <script type="application/ld+json" set:html={JSON.stringify(webSiteJsonLd)} />
  </head>
  <body>
    <a class="skip-link" href="#main-content">Skip to content</a>

    <header class="site-header">
      <div class="shell">
        <a class="brand" href="/" aria-label="LanDeng home">
          <span class="brand-mark" aria-hidden="true">澜灯</span>
          <span class="brand-name">LanDeng</span>
        </a>
        <nav class="site-nav" aria-label="Primary navigation">
          {
            navItems.map((item) => {
              const isCurrent = item.exact
                ? currentPath === item.href
                : currentPath.startsWith(item.href);
              return (
                <a href={item.href} aria-current={isCurrent ? 'page' : undefined}>
                  {item.label}
                </a>
              );
            })
          }
        </nav>
      </div>
    </header>

    <main id="main-content" class="site-main">
      <slot />
    </main>

    <footer class="site-footer">
      <div class="shell">
        <div>
          <p class="footer-brand">澜灯 <span>LanDeng</span></p>
          <p class="footer-note">
            Chinese botanical incense, documented in English with source transparency.
          </p>
        </div>
        <nav class="footer-links" aria-label="Footer navigation">
          <a href="/chinese-incense/">Chinese Incense</a>
          <a href="/ingredients/">Ingredients</a>
          <a href="/wholesale/">Wholesale</a>
          <a href="/oem/">OEM</a>
          <a href="/safety/">Safety</a>
          <a href="/contact/">Contact</a>
        </nav>
        <nav class="footer-links" aria-label="Machine-readable resources">
          <a href="/sitemap-index.xml">Sitemap</a>
          <a href="/llms.txt">llms.txt</a>
          <a href="/data/ingredients.json">Ingredient dataset</a>
          <a href="/data/terminology.json">Terminology dataset</a>
        </nav>
        <p class="copyright">© {new Date().getFullYear()} LanDeng</p>
      </div>
    </footer>
  </body>
</html>

<style is:global>
  :root {
    --ink: #1c1a17;
    --ink-soft: #55514a;
    --paper: #faf7f1;
    --paper-raise: #ffffff;
    --line: #e7e0d4;
    --amber: #b07a3f;
    --amber-soft: #f1e4d2;
    --jade: #5c7260;
    --wood: #8a6b4f;
    --danger: #a33d2f;

    --serif: 'Source Serif 4', 'Noto Serif SC', Georgia, 'Times New Roman', serif;
    --sans: Inter, 'Noto Serif SC', system-ui, -apple-system, BlinkMacSystemFont, sans-serif;

    --max-prose: 45rem;
    --max-shell: 72rem;
    --space-1: 0.5rem;
    --space-2: 1rem;
    --space-3: 1.5rem;
    --space-4: 2rem;
    --space-5: 3rem;
    --space-6: 4.5rem;
    --radius: 8px;
  }

  * {
    box-sizing: border-box;
  }

  html {
    background: var(--paper);
    color: var(--ink);
    font-family: var(--sans);
    font-size: 17px;
    line-height: 1.65;
    scroll-behavior: smooth;
  }

  body {
    margin: 0;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    background:
      radial-gradient(circle at 80% 0%, rgba(176, 122, 63, 0.06), transparent 34rem),
      var(--paper);
  }

  a {
    color: var(--wood);
    text-underline-offset: 0.18em;
    text-decoration-thickness: 1px;
    transition: color 180ms ease;
  }

  a:hover {
    color: var(--ink);
  }

  h1,
  h2,
  h3,
  h4 {
    color: var(--ink);
    font-family: var(--serif);
    font-weight: 600;
    line-height: 1.18;
    letter-spacing: -0.02em;
  }

  h1 {
    font-size: clamp(2.3rem, 5vw, 3.8rem);
    margin: 0 0 var(--space-3);
  }

  h2 {
    font-size: clamp(1.7rem, 3vw, 2.4rem);
    margin: var(--space-6) 0 var(--space-3);
    scroll-margin-top: 5.5rem;
  }

  h3 {
    font-size: 1.35rem;
    margin: var(--space-4) 0 var(--space-2);
    scroll-margin-top: 5.5rem;
  }

  h4 {
    scroll-margin-top: 5.5rem;
  }

  p {
    margin: 0 0 var(--space-3);
  }

  .skip-link {
    position: absolute;
    left: 1rem;
    top: -3rem;
    z-index: 20;
    padding: 0.5rem 0.75rem;
    background: var(--ink);
    color: var(--paper);
    border-radius: var(--radius);
  }

  .skip-link:focus {
    top: 1rem;
  }

  .shell {
    width: min(var(--max-shell), calc(100% - 2.5rem));
    margin-inline: auto;
  }

  .prose {
    width: min(var(--max-prose), 100%);
  }

  .site-header {
    border-bottom: 1px solid var(--line);
    background: var(--paper);
    position: sticky;
    top: 0;
    z-index: 10;
  }

  .site-header .shell {
    min-height: 4rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-3);
  }

  .brand {
    display: inline-flex;
    align-items: baseline;
    gap: 0.65rem;
    color: var(--ink);
    text-decoration: none;
    font-family: var(--serif);
  }

  .brand-mark {
    font-size: 1.35rem;
    font-weight: 600;
    letter-spacing: 0.08em;
  }

  .brand-name {
    font-size: 1.05rem;
    letter-spacing: 0.02em;
  }

  .site-nav {
    display: flex;
    align-items: center;
    gap: 1.35rem;
    font-size: 0.95rem;
  }

  .site-nav a,
  .footer-links a {
    color: var(--ink-soft);
    text-decoration: none;
    transition: color 180ms ease;
  }

  .site-nav a:hover,
  .footer-links a:hover {
    color: var(--ink);
  }

  .site-nav a:hover {
    text-decoration: underline;
    text-decoration-color: var(--amber);
  }

  .site-nav [aria-current='page'] {
    color: var(--ink);
    font-weight: 600;
  }

  .site-main {
    flex: 1 0 auto;
    width: 100%;
  }

  .site-footer {
    margin-top: var(--space-6);
    border-top: 1px solid var(--line);
    background: color-mix(in srgb, var(--amber-soft) 34%, var(--paper));
  }

  .site-footer .shell {
    display: grid;
    grid-template-columns: 1.6fr 1fr 1fr;
    gap: var(--space-4);
    padding-block: var(--space-5) var(--space-4);
  }

  .footer-brand {
    margin: 0 0 var(--space-1);
    font-family: var(--serif);
    font-size: 1.3rem;
    font-weight: 600;
  }

  .footer-brand span {
    font-weight: 400;
    color: var(--ink-soft);
  }

  .footer-note {
    color: var(--ink-soft);
    font-size: 0.94rem;
    max-width: 34rem;
  }

  .footer-links {
    display: flex;
    flex-direction: column;
    gap: 0.45rem;
    font-size: 0.94rem;
  }

  .copyright {
    grid-column: 1 / -1;
    margin: var(--space-2) 0 0;
    color: var(--ink-soft);
    font-size: 0.82rem;
  }

  :focus-visible {
    outline: 2px solid var(--amber);
    outline-offset: 3px;
  }

  .text-link:focus-visible,
  .btn:focus-visible {
    outline-color: var(--ink);
  }

  /* ---- Standalone article pages (wholesale/oem/quote/contact/safety/concepts) ---- */
  .article {
    width: min(var(--max-prose), calc(100% - 2.5rem));
    margin-inline: auto;
    padding-block: var(--space-6);
  }

  .article > h1 {
    font-size: clamp(2rem, 4.5vw, 3rem);
    margin-bottom: var(--space-3);
  }

  .article .lead {
    padding-bottom: var(--space-4);
    border-bottom: 1px solid var(--line);
  }

  .callout {
    margin: var(--space-4) 0;
    padding: var(--space-3) var(--space-4);
    border: 1px solid var(--line);
    border-left: 3px solid var(--amber);
    border-radius: var(--radius);
    background: var(--amber-soft);
  }

  .callout a {
    color: var(--ink);
    text-decoration: underline;
  }

  .callout a:hover {
    color: var(--wood);
  }

  code {
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, 'Liberation Mono', monospace;
    font-size: 0.85em;
    color: var(--ink);
    background: var(--paper);
    border: 1px solid var(--line);
    border-radius: 4px;
    padding: 0.12em 0.4em;
  }

  /* ---- Guide pages (blog/wholesale/oem) ---- */
  .guide-page {
    padding-bottom: var(--space-6);
  }

  .guide-header {
    padding-block: var(--space-4) var(--space-3);
    border-bottom: 1px solid var(--line);
  }

  .breadcrumb {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.85rem;
    color: var(--ink-soft);
    margin-bottom: var(--space-2);
  }

  .breadcrumb a {
    color: var(--ink-soft);
    text-decoration: none;
    transition: color 180ms ease;
  }

  .breadcrumb a:hover {
    color: var(--ink);
  }

  .breadcrumb [aria-hidden] {
    color: var(--line);
  }

  .breadcrumb [aria-current] {
    color: var(--ink);
    font-weight: 500;
  }

  .guide-meta {
    margin: 0 0 var(--space-1);
    font-size: 0.8rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--jade);
  }

  .article-body {
    font-size: 1.02rem;
  }

  .article .lead,
  .lede {
    font-family: var(--serif);
    font-size: 1.28rem;
    line-height: 1.5;
    color: var(--ink-soft);
    margin-bottom: var(--space-4);
  }

  /* ---- Content images ---- */
  img {
    height: auto;
  }

  .article-body img,
  .article img,
  .prose img {
    max-width: 100%;
    height: auto;
    display: block;
    margin: var(--space-4) auto;
    border-radius: var(--radius);
    background: var(--paper-raise);
    box-shadow: 0 1px 2px rgba(28, 26, 23, 0.06), 0 6px 20px -8px rgba(28, 26, 23, 0.12);
  }

  .article-body img[src*='chart'],
  .article-body img[src*='wheel'],
  .prose img[src*='chart'],
  .prose img[src*='wheel'] {
    max-width: min(100%, 34rem);
  }

  /* ---- Homepage hero ---- */
  .hero {
    padding-block: var(--space-6) var(--space-5);
    text-align: center;
  }

  .hero-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-3);
  }

  .hero h1 {
    margin: 0;
  }

  .hero h1 span {
    font-weight: 400;
    color: var(--ink-soft);
  }

  .eyebrow {
    margin: 0;
    font-size: 0.85rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--jade);
  }

  .hero .lede {
    max-width: 40rem;
  }

  .hero img {
    width: min(100%, 62rem);
    height: auto;
    margin: var(--space-2) auto;
    border-radius: var(--radius);
    box-shadow: 0 1px 2px rgba(28, 26, 23, 0.06), 0 6px 20px -8px rgba(28, 26, 23, 0.12);
  }

  .hero-note {
    margin: 0;
    font-size: 0.9rem;
    color: var(--ink-soft);
  }

  /* ---- Buttons & CTAs ---- */
  .text-link,
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.7rem 1.5rem;
    border: 1px solid var(--amber);
    border-radius: 999px;
    background: var(--amber);
    color: var(--ink);
    font-weight: 600;
    font-size: 0.98rem;
    text-decoration: none;
    transition:
      background-color 180ms ease,
      border-color 180ms ease,
      color 180ms ease,
      transform 180ms ease;
  }

  .text-link:hover,
  .btn:hover {
    background: var(--wood);
    border-color: var(--wood);
    color: var(--paper);
    transform: translateY(-1px);
  }

  .text-link:active,
  .btn:active {
    transform: translateY(0);
  }

  /* ---- Sections ---- */
  .section {
    padding-block: var(--space-5);
  }

  .section-label {
    margin: 0 0 var(--space-2);
    font-size: 0.8rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--jade);
  }

  .section-heading {
    max-width: 44rem;
    margin-bottom: var(--space-4);
  }

  /* ---- Lists & blockquote ---- */
  .article-body ul,
  .article-body ol,
  .article ul,
  .article ol,
  .prose ul,
  .prose ol {
    padding-left: 1.4rem;
    margin-bottom: var(--space-3);
  }

  .article-body li,
  .article li,
  .prose li {
    margin-bottom: 0.45rem;
  }

  .article-body blockquote,
  .article blockquote,
  .prose blockquote {
    margin: var(--space-4) 0;
    padding: var(--space-3) var(--space-4);
    border-left: 3px solid var(--amber);
    border-radius: 0 var(--radius) var(--radius) 0;
    background: var(--paper-raise);
    font-family: var(--serif);
    font-style: italic;
    color: var(--ink-soft);
  }

  .article-body blockquote strong,
  .article blockquote strong,
  .prose blockquote strong {
    color: var(--ink);
  }

  .article-body blockquote p:last-child,
  .article blockquote p:last-child,
  .prose blockquote p:last-child {
    margin-bottom: 0;
  }

  /* ---- FAQ ---- */
  .article-body h2#faq {
    margin-top: var(--space-6);
    padding-top: var(--space-4);
    border-top: 1px solid var(--line);
  }

  .article-body h2#faq ~ h3 {
    font-family: var(--sans);
    font-size: 1.06rem;
    font-weight: 600;
    letter-spacing: 0;
    color: var(--wood);
    margin: var(--space-3) 0 var(--space-1);
  }

  .article-body h2#faq ~ h3 + p {
    margin-bottom: var(--space-3);
  }

  .article-body h2#faq ~ p > strong:first-child {
    color: var(--wood);
  }

  /* ---- Tables ---- */
  .article-body table,
  .article table,
  .prose table {
    width: 100%;
    border-collapse: collapse;
    margin: var(--space-4) 0;
    font-size: 0.95rem;
  }

  .article-body th,
  .article th,
  .prose th {
    text-align: left;
    font-weight: 600;
    color: var(--ink-soft);
    font-size: 0.8rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 0.65rem 0.75rem;
    border-bottom: 2px solid var(--line);
  }

  .article-body td,
  .article td,
  .prose td {
    padding: 0.65rem 0.75rem;
    border-bottom: 1px solid var(--line);
    vertical-align: top;
  }

  .article-body tbody tr,
  .article tbody tr,
  .prose tbody tr {
    transition: background-color 180ms ease;
  }

  .article-body tbody tr:hover,
  .article tbody tr:hover,
  .prose tbody tr:hover {
    background: var(--amber-soft);
  }

  @media (max-width: 44rem) {
    .site-header .shell {
      flex-wrap: wrap;
      min-height: auto;
      padding-block: 0.7rem;
      row-gap: 0.55rem;
    }

    .site-nav {
      width: 100%;
      justify-content: space-between;
      gap: 0.5rem;
      font-size: 0.9rem;
    }

    .site-footer .shell {
      grid-template-columns: 1fr;
    }

    .copyright {
      grid-column: auto;
    }

    .hero {
      padding-block: var(--space-4);
    }

    .hero .lede,
    .lede {
      font-size: 1.1rem;
    }

    .article-body table,
    .article table,
    .prose table {
      display: block;
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
      overscroll-behavior-x: contain;
    }

    h2,
    h3,
    h4 {
      scroll-margin-top: 7rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    html {
      scroll-behavior: auto;
    }

    *,
    *::before,
    *::after {
      transition-duration: 0.01ms !important;
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
    }
  }

  @media print {
    .skip-link,
    .site-header,
    .site-footer {
      display: none;
    }

    body {
      background: #fff;
      color: #000;
    }

    a {
      color: #000;
      text-decoration: underline;
    }

    .shell,
    .prose,
    .article,
    .article-layout {
      width: auto;
      max-width: none;
      margin: 0;
    }

    img {
      break-inside: avoid;
      box-shadow: none;
    }
  }
</style>
